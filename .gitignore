# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# typescript build artifacts
*.tsbuildinfo

# production
build/
dist/

# testing
coverage/

# environment — секреты никогда не попадают в git
.env
.env.*
!.env.example

# supabase local
supabase/.branches/
supabase/.temp/

# локальные дампы — не коммитить. Процедура: npm run db:drill
backups/

# os / editor
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# logs
*.log
npm-debug.log*

# temp
.tmp-home/
.venv/
.render/

# Исходные PDF намеренно не в репозитории: 63 МБ навсегда осели бы в истории.
# ВАЖНО: значит, у них нет резервной копии в git. Храните их отдельно.
docs/source/

# Vercel CLI link + local env copies (secrets stay out of git)
.vercel/
.env.local
.env*.local
